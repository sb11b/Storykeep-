# Storykeep web client

Next.js UI for the personal RSS archive. Run it from the repo root instructions in `../README.md`.

```bash
npm install
API_ORIGIN=http://127.0.0.1:18741 npm run dev
```

The app proxies `/api/*` to the FastAPI server.
