diff --git a/backend/app/build-info.json b/backend/app/build-info.json
index be66125..5b90942 100644
--- a/backend/app/build-info.json
+++ b/backend/app/build-info.json
@@ -1 +1 @@
-{"sha": "junior-merge-deploy-v1", "time": "2026-09-22T08:30:00Z"}
+{"sha": "junior-empty-fallback-v1", "time": "2026-09-22T08:50:00Z"}
diff --git a/backend/app/routers/chat.py b/backend/app/routers/chat.py
index 5f71990..8f9173b 100644
--- a/backend/app/routers/chat.py
+++ b/backend/app/routers/chat.py
@@ -1687,10 +1687,33 @@ def _chat(
                 await emit_delta(note)
                 yield chat_service.encode_sse({"delta": note, "stream_status": "writing"})
             if not saw_text:
-                yield chat_service.encode_sse(
-                    chat_service.stream_error_event(504, chat_service.XAI_EMPTY_DETAIL)
-                )
-                return
+                tool_calls_out.clear()
+                async for piece in _stream_xai(None, None):
+                    if cancelled.is_set() or await request.is_disconnected():
+                        cancelled.set()
+                        if piece:
+                            assistant_parts.append(piece)
+                        _persist_assistant("".join(assistant_parts))
+                        return
+                    if not piece:
+                        if not saw_text:
+                            yield chat_service.encode_sse({"stream_status": "thinking"})
+                            await asyncio.sleep(0)
+                        continue
+                    first = not saw_text
+                    await emit_delta(piece)
+                    payload: dict[str, object] = {"delta": piece}
+                    if first:
+                        payload["stream_status"] = "writing"
+                    yield chat_service.encode_sse(payload)
+                    await asyncio.sleep(0)
+                if cancelled.is_set() or await request.is_disconnected():
+                    _persist_assistant("".join(assistant_parts))
+                    return
+            if not saw_text:
+                note = chat_service.EMPTY_REPLY_FALLBACK
+                await emit_delta(note)
+                yield chat_service.encode_sse({"delta": note, "stream_status": "writing"})
             if persist and conversation_id:
                 assistant_text = "".join(assistant_parts).strip()
                 assistant_message_id = _persist_assistant(assistant_text)
diff --git a/backend/app/services/chat.py b/backend/app/services/chat.py
index 3d4e8c6..48182a5 100644
--- a/backend/app/services/chat.py
+++ b/backend/app/services/chat.py
@@ -41,6 +41,10 @@ CHAT_IDLE_AFTER_TOKEN_MAX_SEC = 7200.0
 CHAT_HEALTH_TIMEOUT_SEC = 10.0
 XAI_SILENT_DETAIL = "xAI silent"
 XAI_EMPTY_DETAIL = "Junior returned no text for this turn."
+EMPTY_REPLY_FALLBACK = (
+    "I didn't get a text reply from the model on that turn. "
+    "Try sending again — if it keeps happening, start a fresh chat thread."
+)
 SSE_PADDING = b":" + (b" " * 4096) + b"\n\n"
 # Railway env can still hold retired aliases. Invalid ids hang the stream until a proxy 504.
 _DEAD_MODEL_ALIASES = {
diff --git a/backend/tests/test_chat_silent.py b/backend/tests/test_chat_silent.py
index be37bb7..eea5e3c 100644
--- a/backend/tests/test_chat_silent.py
+++ b/backend/tests/test_chat_silent.py
@@ -266,7 +266,7 @@ class ChatSilentGateTests(unittest.TestCase):
         self.assertIn("Here is a real reply.", response.text)
         self.assertNotIn("returned no text", response.text)
 
-    def test_empty_xai_stream_emits_error_not_done(self):
+    def test_empty_xai_stream_emits_fallback_not_bare_error(self):
         async def empty(*_args, **_kwargs):
             if False:
                 yield ""  # pragma: no cover
@@ -284,8 +284,9 @@ class ChatSilentGateTests(unittest.TestCase):
                 json={"message": "hello", "model": "auto", "reasoning_effort": "auto"},
             )
         self.assertEqual(response.status_code, 200)
-        self.assertIn("returned no text", response.text)
-        self.assertNotIn("data: [DONE]", response.text)
+        self.assertIn(chat_service.EMPTY_REPLY_FALLBACK, response.text)
+        self.assertIn("data: [DONE]", response.text)
+        self.assertNotIn("returned no text", response.text)
 
     def test_empty_piece_after_connect_is_thinking(self):
         async def fake_stream(*_args, **kwargs):
diff --git a/frontend/src/components/grok-pane.tsx b/frontend/src/components/grok-pane.tsx
index 098b06b..8180c8c 100644
--- a/frontend/src/components/grok-pane.tsx
+++ b/frontend/src/components/grok-pane.tsx
@@ -379,7 +379,6 @@ export function GrokPane({
       }
       generatingRef.current = mapped === "generating";
       if (mapped === "queued" || mapped === "thinking") gotDeltaRef.current = false;
-      if (mapped === "writing") gotDeltaRef.current = true;
       setStreamStatus(mapped);
       const turn = normalizeTurnStatus(mapped);
       onUpdate((current) => ({
diff --git a/frontend/src/lib/grok-stream.test.ts b/frontend/src/lib/grok-stream.test.ts
index 0136b79..1074de0 100644
--- a/frontend/src/lib/grok-stream.test.ts
+++ b/frontend/src/lib/grok-stream.test.ts
@@ -287,6 +287,28 @@ test("generating status extends idle past the 60s text timeout", async () => {
   assert.deepEqual(parts, ["Generating the image…\n\n", "done"]);
 });
 
+test("searching alone does not count as the first token", async () => {
+  const encoder = new TextEncoder();
+  const stream = new ReadableStream<Uint8Array>({
+    start(controller) {
+      controller.enqueue(encoder.encode('data: {"stream_status":"searching"}\n\n'));
+      controller.close();
+    },
+  });
+  await assert.rejects(
+    () =>
+      readGrokChatStream(new Response(stream), { onDelta: () => {} }, undefined, {
+        firstByteMs: 80,
+        idleAfterMs: 5000,
+      }),
+    (error: unknown) => {
+      assert.ok(error instanceof ApiError);
+      assert.match(error.message, /xAI silent/);
+      return true;
+    },
+  );
+});
+
 test("heartbeat does not count as the first token", async () => {
   const hangingChunks = [
     'data: {"heartbeat":true}\n\n',
diff --git a/frontend/src/lib/grok-stream.ts b/frontend/src/lib/grok-stream.ts
index d21eb3a..9d54a93 100644
--- a/frontend/src/lib/grok-stream.ts
+++ b/frontend/src/lib/grok-stream.ts
@@ -103,7 +103,6 @@ function parseSsePart(
     idle.ms = Math.max(idle.ms, GROK_STREAM_IMAGE_IDLE_MS);
   }
   if (parsed.stream_status === "searching") {
-    receivedDelta.value = true;
     receivedActivity && (receivedActivity.value = true);
     if (idle) idle.ms = GROK_STREAM_SEARCH_IDLE_MS;
   }
